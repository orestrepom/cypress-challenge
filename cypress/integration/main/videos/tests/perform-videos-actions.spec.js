/// <reference types = "cypress" />

import { VideosPage } from "../page-classes/videos-page";

describe ('videos search suite', () => {
    
    before('initialize page object ', () =>{
       videosPage = new VideosPage()
    })

})